[metadata]
# This can be empty if all metadata is in pyproject.toml
# name = family-tree-backend
# version = 0.1.0

[options]
py_modules =
    app
    config
    models
    routes
# If you had packages, you would use:
# packages = find:
# package_dir =
#     = src
# (Assuming a src layout)
