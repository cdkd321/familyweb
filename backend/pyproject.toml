[project]
name = "family-tree-backend"
version = "0.1.0"
description = "The backend for the Family Tree application."

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
